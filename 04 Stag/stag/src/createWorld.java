import java.util.HashMap;
import org.json.simple.JSONArray;

public class createWorld  {

    // JSONArray is an array of arrays - extra loop over each array
   /*  public ArrayList generateActionsArray(JSONArray actionsArray){
        for(int i = 0; i < actionsArray.length(); ){

        }
        return actionsMap;
    } */


}
